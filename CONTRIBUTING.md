# 贡献指南

## 报告问题
- 使用 Issue 模板，附上目标特征与完整输出（**脱敏后**）
- 漏洞模块相关问题请注明 CVE 编号与目标版本

## 开发流程
```bash
go build -o ssp . && go test ./...
```
- 新漏洞模块：在 `internal/vulns/` 新增文件，实现 `Vuln` 接口并 `init()` 注册（菜单/MCP 自动收录）
- 提交 PR 前确保 `go vet ./...`、`gofmt -l .`、`go test ./...` 全部通过

## 添加新漏洞模块的约定
- 一个漏洞一个文件，Detect 仅做无害判定
- 利用过程需考虑状态恢复与目标业务影响
